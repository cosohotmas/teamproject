# 인수인계 문서 — 프로젝트 컨텍스트 엔지니어링 세팅

작성 시점: 이 세션 종료 시점. 다음 작업자가 이어받기 전에 전체를 한 번 읽어주세요.

## 1. 배경 / 목적

- 프로젝트: **K-Menu Voice Guide** — 영어권 관광객이 한국어 메뉴판을 촬영하고 영어 음성으로 질문하면, 영어 음성으로 재료·맛·맵기·먹는 법을 설명해주는 멀티모달 서비스. 14일 MVP.
- GitHub 레포: `aihuman-7th/proj1-c` (private, AI Human 7기 조별 프로젝트, 조 = C조)
- 팀 구성(현재 기준): **Claude Code 2명 · Codex 1명 · Cline 1명**, 조장 = 이 세션 작업자. (세션 초반엔 "Claude Code 1명 + Codex 3명"으로 알고 시작했다가 중간에 실제 구성이 이걸로 확인되어 바뀌었습니다. 과거 대화 로그를 다시 볼 일이 있으면 이 변경점 감안.)
- 목적: 팀원들이 `.claude`/`.codex` 설정을 모를 뿐 아니라 조장도 개념만 아는 상태에서, AI 코딩 에이전트(Claude Code, Codex, 추후 Cline)가 이 프로젝트에서 **일관되게, 안전하게, 토큰 낭비 없이** 동작하도록 지침·설정을 세팅하는 작업.
- 핵심 문제의식: 팀원마다 공통 지침 없이 작업하면 추론 방식이 달라 결과 편차가 크고(개발 초반엔 오히려 괜찮았지만, 본 개발 단계부터는 병합이 어려워짐), 토큰도 낭비됨.

## 2. 현재 작업 위치 (중요)

**이 폴더(`/Users/jeong-un/Documents/Projects/Main_1_projects/`)는 실제 GitHub 레포가 아니라 로컬 초안 자리입니다.** git 저장소도 아직 아닙니다. 실제 레포 `aihuman-7th/proj1-c`에는 이미 `AGENTS.md`, `CLAUDE.md`, `docs/specs/_example.md`, `.github/`, `.env.example`, `README.md`, `.gitignore`가 존재하며(팀원 kimsanguine이 SDD 스캐폴딩 형태로 미리 세팅), 이 폴더의 파일들은 그 위에 **덮어쓰기/추가할 개정판**입니다.

**아직 실제 레포에 반영(커밋/푸시)되지 않았습니다.** 다음 작업자가 할 일 중 하나가 이 파일들을 실제 레포로 옮기는 것입니다.

## 3. 지금까지 만든 것 — 파일별 설명

### `AGENTS.md`
Claude Code·Codex(추후 Cline)가 공통으로 읽는 지침 파일. 원래 팀원이 만든 스캐폴딩(Spec 워크플로우, Out of Scope 원칙, 완료 기준)에 아래를 추가:
- 프로젝트 개요, 기술 스택(확정 목록 + "아직 미확정" 인용구 블록으로 후보 분리 — 기술검증 끝나면 옮기거나 삭제하는 규칙 명시)
- 구현 원칙(임의로 새 로직/라이브러리 도입 금지, 확신 없으면 질문)
- Git 규칙(main 직접 push 금지, force-push 금지, 위험 명령 사전 확인)
- 보안 원칙(시크릿 하드코딩 금지, `.env` 사용, Secret Scan CI 있음, **PR merge는 조장만 + 1명 이상 approve 필요** — 이건 조장이 직접 추가함)

기술 스택·Git 규칙·보안 원칙 중 상당수는 실제 레포의 `README.md`(강사가 이미 세팅한 협업 규칙 문서)를 읽고 교차 확인해서 반영한 내용입니다. `docs/specs/_example.md`(SDD Spec 템플릿)도 실제 레포에서 확인했으나 이 로컬 폴더에는 없음(정상 — 이 폴더는 전체 미러가 아님).

### `CLAUDE.md`
`@AGENTS.md` 한 줄. Claude Code 전용 import 문법으로, AGENTS.md 내용을 세션 시작 시 확실하게 불러오기 위함(원래 팀원이 만든 버전은 마크다운 링크뿐이라 모델이 "알아서 읽어야" 하는 불확실한 방식이었음 — 이번에 수정).

### `.claude/settings.json`
Claude Code 전용, 프로젝트 공유 설정(레포 커밋 대상).
- `model`: `claude-sonnet-5` 고정
- `permissions.allow`: 자주 쓰는 안전한 명령(git status/diff/log/add/commit/push, pytest, pip install, huggingface-cli download 등) 자동 승인
- `permissions.deny`: **현재 빈 배열([])입니다 — 아래 5번 "미해결 사항" 꼭 확인**
- `hooks.PreToolUse`: 두 개 연결됨 (아래 hook 파일 참고)

### `.claude/hooks/git-bash-guard.sh`, `.claude/hooks/secret-scan.sh`
Bash 명령어(및 Write/Edit로 작성되는 파일 내용)를 실행 직전에 가로채서 아래를 차단하는 hook:
1. main 브랜치 직접 commit/push
2. force-push 계열(`--force`/`-f`/`--force-with-lease`, 위치 무관)
3. `git reset --hard`
4. `git clean -f`
5. `git branch -D`
6. `rm -rf`
7. `.env` 파일 git add
8. 모델 체크포인트·대용량 데이터 git add
9. (secret-scan.sh만) 코드에 하드코딩된 API 키 패턴(`sk-`, `AKIA`, PEM 헤더) 작성 시도

**주의**: `git-bash-guard.sh`는 `jq`에 의존하며, `jq`가 없거나 파싱이 실패하면 **조용히 허용(fail-open)**됩니다. 아래 5번 참고.

### `.codex/config.toml`
Codex 전용, 프로젝트 공유 설정.
- `model = "gpt-5.6-terra"`, `model_reasoning_effort = "medium"` (팀원이 직접 확인 후 채운 값)
- `[features] hooks = true` (방어적으로 명시. 참고: 예전에 이 대화에서 `codex_hooks`라는 플래그명을 언급한 적 있는데 **틀린 정보였음** — 실제 설치된 codex-cli 0.146.0을 직접 실행해서 확인한 정확한 키는 `hooks`)

### `.codex/hooks.json`, `.codex/hooks/guard_bash.py`
Codex용 PreToolUse hook. 위 1~8번과 동일한 항목을 Python(외부 의존성 없음, 표준 라이브러리만 사용)으로 구현. 실제 codex-cli 바이너리를 격리 환경에서 실행해 로직·경로탐색 모두 검증 완료. 단, 9번(시크릿 하드코딩)은 미구현 — Codex의 PreToolUse가 Bash 외 이벤트(Write/Edit 등)까지 지원하는지 확인 못 해서 범위에서 제외함.

### `.codex/for-personal-setup/README.md`
**레포에는 올라가지만 자동 적용되지 않는 부분**에 대한 안내. Codex는 프로젝트 폴더 안의 `.codex/` 설정이 실제 동작하려면, 사용자가 자기 개인 전역 `~/.codex/config.toml`에 이 프로젝트 경로를 `trust_level = "trusted"`로 **직접 등록**해야 함(보안 설계상 프로젝트 파일 스스로 "나를 신뢰하라"고 선언 못 하게 막아둔 것). 이 등록은 git으로 절대 전달 안 되므로, Codex 쓰는 팀원 1명에게 이 README 내용을 구두/채팅으로 직접 전달해야 함.

### `.gitignore`
팀원 원본(env/secrets, Python, Node, OS, 로그) + 이번에 추가: 모델 체크포인트(`*.pt`/`.pth`/`.ckpt`/`.safetensors`), 대용량 데이터 폴더(`data/raw/`, `checkpoints/`), Gradio `flagged/`, 오디오 파일(`*.wav`/`.mp3`/`.m4a`/`.flac`, 단 데모 샘플용 `samples/` 폴더는 예외 처리).

## 4. 왜 이렇게 설계했는지 (핵심 판단 근거)

- **AGENTS.md만 Claude Code·Codex 공통.** `.claude/settings.json`과 `.codex/config.toml`은 서로 완전히 다른 프로그램의 설정이라 서로 대체 불가 — 한쪽만 만들면 다른 툴 쓰는 팀원한텐 0% 효과. 그래서 "프로그램적으로 강제 안 되는 행동 규칙"은 전부 AGENTS.md 텍스트로도 넣어뒀음(중복 같아 보여도 의도적).
- **skills / 서브에이전트 / AGENTS.md 파일 쪼개기는 의도적으로 안 만듦.** 4인·14일 규모에 오버스펙이라 판단. 실제로 반복되는 필요가 생기면 그때 추가.
- **GitHub 브랜치 보호(관리자 설정)는 시도했으나 불가능했음** — 현재 로그인 계정이 이 레포 관리자 권한이 없음(`You don't have access to repository options`). 레포 생성자(아마 kimsanguine)가 관리자일 가능성 높음. 관리자 권한 있는 사람이 확인되면 GitHub 설정에서 "main force-push 금지"를 서버 단에서 걸어주는 게 훨씬 확실한 이중 방어가 됨 — 지금은 AGENTS.md 텍스트 + hook만으로 방어 중.

## 5. 미해결 / 다음 작업자가 반드시 확인해야 할 것

1. **[가장 중요, 답변 대기 중] `.claude/settings.json`의 `permissions.deny`가 현재 비어 있음.** 원래 있던 6개 항목(force-push, reset --hard, clean -f, branch -D, rm-rf 등)을 서브에이전트가 "hook이 대체하니 중복"이라며 지웠는데, 실제 검토 결과 `git-bash-guard.sh`가 `jq` 의존성 때문에 **`jq`가 없는 환경에서는 조용히 무력화**될 수 있음을 확인함(fail-open 구조). 조장에게 "deny 목록을 이중 방어로 복원할지" 질문해뒀으나 **세션 종료 시점까지 답을 못 받음.** 다음 작업자가 조장에게 재확인 후 처리 필요. (참고: `.codex/hooks/guard_bash.py`는 Python 표준 라이브러리만 써서 이런 외부 의존성 리스크가 없음 — Claude Code 쪽만의 문제.)
2. **시니어 평가 서브에이전트가 제안한 AGENTS.md 개선 2건, 아직 미반영:**
   - Codex 사용자에게는 AGENTS.md가 프로그램적 강제 없는 "유일한 안전장치"라는 점이 문서 안에 전혀 명시되어 있지 않음 — 한 줄 추가 고려.
   - "Spec을 먼저 작성하세요"라는 문장이 권고 톤이라 의무인지 불명확함. Spec 없이 바로 구현 요청이 왔을 때 에이전트가 어떻게 해야 하는지, 그리고 사용자의 채팅 지시가 문서 규칙과 충돌할 때 우선순위가 무엇인지 — 둘 다 명시 규칙 없음.
3. **Cline 작업 전혀 시작 안 함(최후순위로 미룸).** Cline이 AGENTS.md를 네이티브로 읽는지, CLINE.md 같은 별도 파일이 필요한지, hook 개념이 있는지 — 전부 미조사 상태. 다른 두 서브에이전트처럼 리서치부터 시작해야 함(추측 금지 — Codex 때도 리서치만으론 정보가 엇갈려서 실제 바이너리로 검증해야 정확했음).
4. **아직 실제 GitHub 레포에 반영 안 됨.** 이 폴더의 모든 파일을 `aihuman-7th/proj1-c`로 옮겨서 브랜치 만들고 PR 올리는 작업이 남아있음. 조장 계획은 "실제 작업 시작 직전에 푸시"였음 — 그 전까지는 이 폴더 파일을 드래그해서 팀원들에게 임시 전달하는 것도 가능(단, 한번 팀원이 작업 시작한 뒤에는 반드시 git으로 동기화해야 함 — 안 그러면 편차 문제가 재발함).
5. **Codex 쓰는 팀원 1명에게 `.codex/for-personal-setup/README.md` 내용을 구두/채팅으로 반드시 전달해야 함.** git으로는 절대 자동 전달 안 되는 부분.
6. **사소한 미해결**: `.claude/settings.json`의 `permissions.allow`에 브랜치 생성(`git checkout -b`)만 있고 브랜치 전환(`git checkout <branch>`/`git switch`)이 없어 매번 승인 프롬프트가 뜰 수 있음(오류 아니고 불편함 수준). `.gitignore`의 "Node" 관련 항목은 이 프로젝트가 Python 전용이라 실질적으로 안 쓰임(해는 없음).
7. **Claude Code hook의 `PreToolUse` 설정이 세션 도중 바로 적용되는지, 재시작해야 적용되는지 미확인.** 다음 새 세션에서 실제로 위 1~9번 명령어를 시도해서 재검증 권장.

## 6. 다음에 할 일 제안 순서

1. 조장에게 5-1번(deny 목록 복원 여부) 답 받기 → 처리
2. 5-2번(AGENTS.md 개선 2건) 반영 여부 결정
3. Cline 리서치 → 필요하면 세 번째 서브에이전트로 지침/hook 작업
4. 이 폴더 전체를 실제 레포에 옮겨서 브랜치 → PR → merge
5. Codex 팀원에게 개인 설정 안내 전달
6. 새 세션에서 Claude Code hook 실제 동작 재검증
